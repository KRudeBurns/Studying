/*
2. (Practice) Three integer arguments are to be used in a call to a function named time(). Write a suitable function header for time(), assuming that time() accepts these variables as the reference parameters sec, min, and hours and returns no value 
to its calling function.
*/

#include <iostream>
#include <string>
#include <iomanip>
#include <cmath>
using std::string;
using std::cout;
using std::cin;
using std::endl;
using std::setw;

void time(int& hour, int& min, int& sec);

int main()
{
   
}